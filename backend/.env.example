DATABASE_URL=postgresql+psycopg2://user:password@localhost:5432/device_scan

# 企业微信（不配置则 H5 使用占位用户；配置后首次登录用户需在 DB 中把 users.role 改为 device_admin 或 sys_admin 才能用后台与导出）
# 详细步骤见项目 docs/WECOM_SETUP.md：企微后台获取企业ID/AgentId/Secret、配置可信域名，此处填 WECOM_CORP_ID（注意是 CORP_ID 不是 CORPID）
WECOM_CORP_ID=
WECOM_AGENT_ID=
WECOM_SECRET=
# 后端访问地址，必须与企微「可信域名」一致（生产/穿透用 https），本地仅浏览器测试可留 http://127.0.0.1:8000
BASE_URL=http://127.0.0.1:8000

# JWT（生产环境务必修改，且设置 ENVIRONMENT=production 时若未改会拒绝启动）
JWT_SECRET=change-me-in-production

# 生产环境设为 production，启动时将校验 JWT_SECRET 不可为默认值
# ENVIRONMENT=production

# 院内访问控制（可选）：仅允许以下来源访问 /admin、/docs、管理员登录接口；留空则不校验
# ALLOWED_ADMIN_ORIGINS=https://admin.xxx.edu.cn,https://192.168.1.1
# ALLOWED_ADMIN_IPS=192.168.0.0/16,10.0.0.1

# 企业微信 API 请求超时（秒）
# WECOM_HTTP_TIMEOUT=10.0

# 本地管理员首次登录（可选）：设置后首次用该账号密码在后台登录将自动创建 sys_admin 用户
# ADMIN_USERNAME=admin
# ADMIN_PASSWORD=your-secure-password

